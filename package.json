{
  "name": "thomashartdev-profile",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Self-updating GitHub profile README",
  "scripts": {
    "build:feed": "node scripts/update-feed.mjs",
    "plan:pins": "node scripts/pin-repos.mjs",
    "build:banner": "node scripts/build-banner.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
